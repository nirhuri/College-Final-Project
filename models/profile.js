class Profile {
    constructor(key, userName, userId, pic, title, about) {
      this.key = key;
      this.userName = userName;
      this.userId = userId;
      this.pic = pic;
      this.title = title;
      this.about = about;
    }
  }
  
  export default Profile;